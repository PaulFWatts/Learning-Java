import java.util.Scanner;

/**
 * Reverses the digits of number.
 */
public class ReverseDigitsStart {

    public static void main (String[] args) {
        Scanner sc = new Scanner(System.in);
        int number, lastDigit;
        int reverse = 0;
        
        System.out.print("Enter a positive integer: ");
        number = sc.nextInt();
        //TASK: Add a post-tested loop to:
        //      BODY: find the last digit of number and add it to the reverse
        //      UPDATE: remove the last digit of number (making number ~10 times smaller)
        //      and keep going while a TEST shows number > 0
        
        
        
        
        
        System.out.println ("That number reversed is " + reverse);
    }

}
