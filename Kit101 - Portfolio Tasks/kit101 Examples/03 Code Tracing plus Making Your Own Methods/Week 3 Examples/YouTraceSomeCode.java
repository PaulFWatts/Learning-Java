/**
 * Sample code tracing problem for the class to solve.
 * @author James Montgomery
 */
public class YouTraceSomeCode {

    public static void main(String[] args) {
        double x, y;
        int p = 7;
        int q = 3;

        x = p / 2.0;
        y = p / 2;
        p = 25 % p;
        p = (p + q) * (p - q) + 2;
    }
    
}